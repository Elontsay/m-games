services:
  - type: web
    name: elons-math-game
    env: node
    buildCommand: npm install
    startCommand: node server.js
    plan: hobby
    envVars:
      - key: NODE_ENV
        value: production
      - key: ADMIN_PASSWORD
        sync: false   # set this in the Render dashboard — do not commit the real value
